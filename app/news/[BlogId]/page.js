import React from 'react';

// import BlogPage from '../components/BlogPage';

const Page = ({params}) => {
    return (
        <div>
           {params.id} 
        </div>
    );
}

export default Page;
